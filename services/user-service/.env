# Database connection (Postgres)
# Replace username, password, host, port, and dbname with your local values or CI secrets.
DATABASE_URL="postgresql://gupjob:gupjob_pass@localhost:5432/gupjob_dev?schema=public"

# Mentor service base URL (used when recommending mentors)
MENTOR_SERVICE_URL="http://localhost:4100"

# Server configuration
PORT=4000
NODE_ENV=development